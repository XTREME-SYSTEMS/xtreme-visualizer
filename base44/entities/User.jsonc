{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "company_name": {
      "type": "string",
      "description": "Company name used on bids, proposals, and branding"
    },
    "logo_url": {
      "type": "string",
      "description": "Uploaded company logo URL \u2014 reused across bids, email templates, digital cards, and brochures"
    },
    "phone": {
      "type": "string",
      "description": "Contact phone for bids and digital cards"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}