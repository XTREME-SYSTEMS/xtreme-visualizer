{
  "name": "MarketPrice",
  "type": "object",
  "properties": {
    "trade": {
      "type": "string",
      "description": "Trade or service looked up"
    },
    "zip_code": {
      "type": "string"
    },
    "low": {
      "type": "number"
    },
    "mid": {
      "type": "number"
    },
    "high": {
      "type": "number"
    },
    "unit": {
      "type": "string",
      "description": "e.g. per sq ft, per project"
    },
    "summary": {
      "type": "string"
    },
    "confidence": {
      "type": "string"
    },
    "chosen_range": {
      "type": "string",
      "enum": [
        "low",
        "mid",
        "high"
      ]
    },
    "adders": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "label": {
            "type": "string"
          },
          "type": {
            "type": "string"
          },
          "amount": {
            "type": "number"
          }
        }
      }
    },
    "final_price": {
      "type": "number"
    }
  },
  "required": [
    "trade",
    "zip_code"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}